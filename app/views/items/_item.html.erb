<h2>To-Do list</h2>

<div class="row">

  <div class="col-md-8">
    <div class="media">
      <ol>
        <% @items.each do |item| %>
          <li><%= item.name %></li>
          <% end %>
      </ol>
    </div>
  </div>
</div>
